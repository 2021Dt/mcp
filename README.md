# AI 日语陪练系统骨架

该仓库仅提供 FastMCP 驱动的 AI 日语陪练系统项目骨架，所有模块都为占位实现，等待后续填充业务逻辑。

## 目录结构

- `server.py`：FastMCP 服务器入口
- `mcp_app.py`：创建 FastMCP 实例的统一入口
- `core/`：核心逻辑模块（会话、课程、场景、用户状态等）
- `tools/`：MCP 工具注册模块
- `examples/`：命令行示例脚本
- `data/`：样例课程、场景与用户状态目录

## 使用说明

当前版本仅包含占位代码，后续可在各模块中逐步实现真实逻辑。
